# ============================================
# CRITICAL: PINNED VERSIONS - DO NOT MODIFY
# These versions are compatible with PyTorch 2.2.0
# ============================================

# Core dependencies (pinned versions)
soundfile==0.12.1
scipy==1.14.1
librosa==0.10.2
omegaconf==2.3.0
huggingface_hub==0.23.0
safetensors==0.4.5
accelerate==0.33.0
einops==0.8.0
ToJyutping==3.2.0
g2p_en==2.1.0
g2pM==0.1.2.5
nltk==3.8.1
demucs
transformers==4.36.2

# Audio and ML dependencies
tokenizers
sentencepiece
sqlalchemy
alembic
aiohttp
av
gitpython
toml
tqdm
psutil
blake3
kornia
spandrel
pydantic
PyOpenGL
glfw

# Jupyter and notebook support
jupyter
jupyterlab
ipykernel

# Note: comfy-kitchen and numpy are pinned in Dockerfile, not here
# to ensure they are installed BEFORE other dependencies
